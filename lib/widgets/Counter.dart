import 'package:flutter/material.dart';

class CharCounter extends StatelessWidget {
 
  @override
  Widget build(BuildContext context) {
    return Text("05/20",style: TextStyle(
      color: Colors.grey[300],
      fontSize: 25,
    ),);
  }
}